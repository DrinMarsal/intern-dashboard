<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Intern Dashboard</title>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #f2f2fb;
        margin: 0;
        padding: 0;
        display: flex;
      }

      .sidebar {
        width: 300px;
        background-color: white;
        height: 100vh;
        padding: 20px;
        box-sizing: border-box;
      }

      .sidebar a {
        display: block;
        color: black;
        text-decoration: none;
        padding: 10px;
        margin-bottom: 10px;
        border-radius: 5px;
        background-color: #f2f2fb7e;
        transition: background-color 0.3s;
      }

      .sidebar a:hover {
        background-color: rgba(102, 187, 235, 0.3);
        color: #000;
      }

      .separator {
        width: 100%;
        border-bottom: 2px solid rgba(102, 187, 235, 0.3);
        margin: 15px 0;
      }

      .main-content {
        flex-grow: 1;
        padding: 20px;
        overflow: hidden;
      }

      .sidebar a.active {
        background-color: #1f5ea7;
        color: white;
      }

      .sidebar-img {
        width: 60%; /* Jika ingin gambar berbentuk lingkaran */
        margin: 20px; /* Memberi jarak antara gambar dan link */
      }

      /* Rest of the previous CSS remains the same */
      /* (Copy entire previous CSS block here) */
    </style>
  </head>
  <body></body>
</html>
